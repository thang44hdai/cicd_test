name: Flutter CI/CD

on:
  push:
    branches:
      - main # Thay đổi nhánh theo dự án của bạn

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
    - name: Run tests
      run: flutter run

